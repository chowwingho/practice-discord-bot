## Initial Requirements

**V1 - Basic Bot**
- Bot joins test server
- Responds to a simple command (like `!hello`)
- Proves the connection works

**V2 - Standup Features** (future)
- Posts scheduled daily message
- Creates thread for responses
- Sends summary

**Out of Scope for Now**
- Complex scheduling
- Database storage
- Multiple servers